Collection of react components that can be used with the shadcn cli tool to add them to projects.

TODO: while this acts as the collection for the react components, it should also be a project that is set up to build and navigated to the different pages with tanstack router. It should also test all the different types of states the component could be on on the dedicated development pages for each. Uses zod, zustand and miragejs (for mocking) when needed. As well as TanStack Query.
